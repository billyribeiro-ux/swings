# Claude Code Guide

This file is intentionally a thin alias. The canonical agent-facing
guide for this repository — covering Svelte MCP usage, code conventions,
and tooling expectations — lives in **[`AGENTS.md`](./AGENTS.md)**.

The `AGENTS.md` filename is the de-facto cross-tool convention (GitHub
Copilot, Cursor, Codex, Aider, Claude Code, Continue, etc. all read it),
so we keep one document instead of forking instructions per agent.

If you are Claude Code, read `AGENTS.md`.
