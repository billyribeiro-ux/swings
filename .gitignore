node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env (never commit real secrets — templates stay as *.example only)
.env
.env.*
!.env.example
!.env.test
# Rust API local config (also listed in backend/.gitignore — keep both)
backend/.env
backend/.env.*
!backend/.env.example

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
# Playwright
test-results
playwright-report
.env*.local

# Claude Code session/IDE state — runtime locks, worktree scratch.
# `.claude/settings.json` IS committed (project-shared MCP-enforcement
# hooks); only the per-session runtime caches are ignored.
.claude/*
!.claude/settings.json
