node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env (never commit real secrets — templates stay as *.example only)
.env
.env.*
!.env.example
!.env.test
# Rust API local config (also listed in backend/.gitignore — keep both)
backend/.env
backend/.env.*
!backend/.env.example

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
# Playwright
test-results
playwright-report
.env*.local

# Claude Code worktree scratch
.claude/worktrees/
